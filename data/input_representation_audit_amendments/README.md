# Dated post-extraction review amendments

These author-led audit records update the public atlas view while preserving the
frozen taxonomy, source snapshots, route IDs and original classifications. They
are applied by `scripts/apply_atlas_review_amendments.py` after the unchanged
method-locked atlas builder. The Pages workflow verifies the projection before
deployment.

## P023, 2026-09-06

The author accepted an unresolved subtype for the teacher gene-feature route in
RVQ-Alpha v1. The family remains text-native. The route belongs to a replaceable
auxiliary teacher constructing training data; its lifecycle is described relative
to that consuming model. The exact prompt serialization remains unconfirmed.

The earlier quote concerned the RVQ embedding interface. The reviewed view uses
the v1 teacher-synthesis paragraph, with a canonical Markdown hash/line and a
version-pinned article link. Earlier page/item references are preserved with the
superseded quote and are absent from the replacement provenance because the native
Docling document was not available for remapping. The original classification
and quote remain accessible in the public route history.

The exact serialized prompt and verified author repository were not located in
the source search. v2 (2026-08-06) supplies later clarification about a privileged
teacher evidence card and separate student gene injection. It does not silently
replace the audited v1. The hypothetical example previously used in discussion
and the generic F1.L2 illustrative prompt are excluded as classification evidence.

One route moves from a definite subtype to a family-scoped uncertainty state.
The five families and 15 subtype definitions are unchanged. The reviewed display
contains 584 subtype-assigned routes plus one unresolved-subtype route, across
the same 55 records, 54 studies, 109 models and 467 configurations. A graph group
for unresolved annotation makes 48 display groups. Corpus-level scientific
analyses based on the frozen original inventory retain their original denominators
and require explicit version attribution.

The recorded human confirmation covers acceptance of subtype uncertainty.
Evidence inspection and the teacher-role/lifecycle clarification are identified
as author-led AI-assisted work. No additional human confirmations are inferred.

This update does not run inference, reprocess Docling, change crops, open the
audit key, or change article eligibility/PRISMA counts. Future amendments must
name the expected prior values and cite hash-verified evidence. A stale expected
value stops application. Removing an amendment requires an explicit new review
and rebuilding from the frozen base.
